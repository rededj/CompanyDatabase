<Window x:Class="BDcource.ProductEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Продукт" Height="260" Width="400" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Background="{StaticResource BackgroundColor}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Name ="Header" Grid.Row="0" Grid.ColumnSpan="2" 
                   Style="{StaticResource TitleTextBlock}" Margin="0,0,0,15"><Run Language="ru-ru" Text="Добавление"/><Run Text=" продукта"/></TextBlock>

        <Label Grid.Row="1" Grid.Column="0" Content="Название:" FontFamily="Tahoma" FontSize="12" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Name="txtName" Margin="0,5,0,5" FontFamily="Tahoma" FontSize="12"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Стоимость:" FontFamily="Tahoma" FontSize="12" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Name="txtCost" Margin="0,5,0,5" FontFamily="Tahoma" FontSize="12"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Кол-во операций:" FontFamily="Tahoma" FontSize="12" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Name="txtOperationsRequired" Margin="0,5,0,5" 
                 IsReadOnly="True" ToolTip="Изменяется автоматически" FontFamily="Tahoma" FontSize="12"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button Name="btnOk" Content="OK" Style="{StaticResource ClassicButton}" Width="80" Margin="5" Click="BtnOk_Click"/>
            <Button Name="btnCancel" Content="Отмена" Style="{StaticResource ClassicButton}" Width="80" Margin="5" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>