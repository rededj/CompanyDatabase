<Window x:Class="BDcource.Features.Reports.Query3Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Использование инструментов по типам" Height="400" Width="600" WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundColor}">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="181*"/>
            <ColumnDefinition Width="104*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlock}" Margin="0,0,0,15" Grid.ColumnSpan="2"><Run Language="ru-ru" Text="Типы инструментов по использованию"/></TextBlock>

        <DataGrid Name="dgResult" IsReadOnly="True"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  CanUserReorderColumns="False" SelectionMode="Single" RowHeaderWidth="0" Grid.ColumnSpan="2" Margin="0,34,0,0" Grid.RowSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Тип инструмента" Binding="{Binding ToolTypeName}" Width="*"/>
                <DataGridTextColumn Header="Количество использований" Binding="{Binding UsageCount}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="Закрыть" Style="{StaticResource ClassicButton}" Width="80" HorizontalAlignment="Right" Margin="0,15,0,0" Click="Close_Click" Grid.Column="1"/>
    </Grid>
</Window>