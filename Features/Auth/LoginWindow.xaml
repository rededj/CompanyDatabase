<Window x:Class="BDcource.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="250" Width="350" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundColor}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Вход в систему" 
                   FontFamily="Tahoma" FontSize="18" FontWeight="Bold" 
                   Foreground="{StaticResource PrimaryColor}" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Логин:" FontFamily="Tahoma" FontSize="12" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Name="txtLogin" FontFamily="Tahoma" FontSize="12" Margin="0,0,0,5"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Пароль:" FontFamily="Tahoma" FontSize="12" VerticalAlignment="Center"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" Name="txtPassword" FontFamily="Tahoma" FontSize="12" Margin="0,26,0,3" Grid.RowSpan="2"/>

        <Button Grid.Row="3" Name="btnLogin" Content="Войти" 
                Style="{StaticResource ClassicButton}" Width="100" Height="32" 
                Click="BtnLogin_Click" HorizontalAlignment="Left" Grid.Column="1" Margin="25,13,0,14" Grid.RowSpan="2"/>

        <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Name="tbError" 
                   Foreground="Red" TextAlignment="Center" 
                   FontFamily="Tahoma" Margin="0,38,0,-28" FontWeight="Bold" FontSize="14"/>
    </Grid>
</Window>