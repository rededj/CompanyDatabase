<Window x:Class="BDcource.Helpers.CustomMessageBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сообщение" Height="180" Width="400" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False" Background="{StaticResource BackgroundColor}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Width="32" Height="32" Margin="36,0,0,0" 
                Background="{StaticResource PrimaryColor}" CornerRadius="16" 
                HorizontalAlignment="Left" VerticalAlignment="Center" Grid.ColumnSpan="2">
            <TextBlock x:Name="iconText" Text="!" Foreground="White" FontSize="20" 
                       FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <TextBlock Grid.Column="1" x:Name="txtMessage" TextWrapping="Wrap" 
                   FontFamily="Tahoma" FontSize="14" VerticalAlignment="Center" FontWeight="SemiBold" Margin="78,0,0,0"/>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button x:Name="btnOk" Content="OK" Style="{StaticResource ClassicButton}" 
                    Width="90" Height="30" Margin="5" Click="BtnOk_Click"/>
            <Button x:Name="btnYes" Content="Да" Style="{StaticResource ClassicButton}" 
                    Width="80" Height="30" Margin="5" Click="BtnYes_Click" Visibility="Collapsed"/>
            <Button x:Name="btnNo" Content="Нет" Style="{StaticResource ClassicButton}" 
                    Width="80" Height="30" Margin="5" Click="BtnNo_Click" Visibility="Collapsed"/>
            <Button x:Name="btnCancel" Content="Отмена" Style="{StaticResource ClassicButton}" 
                    Width="80" Height="30" Margin="5" Click="BtnCancel_Click" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Window>